# Registry of algos. Programs live in programs/<CITY>/.
# status: dev | paper | live | off
# Edited by `tracer algo set NAME --status ...` — which touches this file only.

strategies:
  ROME:
    universe: ROME
    status: dev
    enabled: true
    description: ETF pairs trading across commodity, rates, credit and FX proxies.
